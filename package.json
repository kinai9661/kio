{
  "name": "kio-gateway",
  "version": "1.0.0",
  "description": "OpenAI Compatible Image Generation API Gateway",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "openai": "^4.24.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
